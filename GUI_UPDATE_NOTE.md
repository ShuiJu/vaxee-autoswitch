# GUI Update Note

这份 note 用于把当前项目里的 CLI 工具改造成轻量级 Windows 托盘 GUI，并把同样的思路复用到别的项目。

## 目标

- 保留原有后台自动工作逻辑，不因为加 GUI 而丢失核心功能。
- Windows 下启动时不弹出 CLI 控制台窗口。
- 程序以系统托盘常驻方式运行。
- 提供一个轻量设置窗口，而不是引入重型 GUI 框架。
- 对配置的修改要“点击即生效”，也就是点击按钮后立刻写配置文件。
- 整体实现优先追求极低性能开销。

## 核心设计原则

- GUI 只是外壳，后台核心逻辑必须继续独立运行。
- 不要把业务逻辑塞进按钮事件里，按钮只负责更新配置。
- 配置文件仍然作为真实来源，GUI 只是配置编辑器。
- 点击 GUI 按钮后，先写配置文件，再唤醒后台逻辑尽快应用。
- 后台线程保持低优先级，避免影响前台使用体验。
- 避免引入 Electron、WebView 全家桶、重量级跨平台 GUI 框架。
- 优先使用原生 Win32 控件和消息循环，降低内存和 CPU 占用。

## 这次改造包含的点

### 1. 保留后台自动切换能力

- 保留原本“周期性检查前台进程并自动切换设备配置”的能力。
- 后台逻辑仍然按配置中的轮询间隔运行。
- 命中白名单时应用 `hit` 配置。
- 未命中白名单时应用 `default` 或 `miss` 配置。
- 只有目标配置发生变化时才真的下发设备设置，避免无意义重复操作。

### 2. 抽离后台核心

- 把原本写在 `main` 里的后台循环抽成独立的 app/core 层。
- GUI 和控制台入口都只调用这层核心逻辑。
- 这样后续换 UI 时不需要再碰设备切换逻辑。

建议结构：

- `app_core.go`
  负责配置加载、后台循环、唤醒机制、配置更新。
- `app_support.go`
  放公共辅助函数、优先级设置、轮询执行逻辑。
- `main_windows.go`
  Windows GUI 入口。
- `main_other.go`
  非 Windows 下的控制台入口。

### 3. 配置写回策略

- 增加 `saveConfig(...)` 能力。
- GUI 每次按钮点击后直接重写配置文件。
- 写配置建议使用“先写临时文件，再 rename 替换”的方式，避免半写入状态。
- 更新完成后重新加载配置，保证内存状态和磁盘一致。
- 对配置写操作加互斥锁，避免后台重载和 GUI 写入打架。

### 4. GUI 交互要求

- 使用托盘图标常驻。
- 左键托盘图标：显示/隐藏设置窗口。
- 右键托盘图标：弹出菜单。
- 右键菜单至少包含：
  - `打开设置`
  - `退出`

设置窗口要求：

- 能切换当前正在编辑的是 `Hit` 还是 `Miss(Default)`。
- 用按钮或单选按钮图形化修改：
  - 性能模式
  - 回报率 / Polling Rate
  - 轨迹模式
- 点击按钮后立即写入配置文件。
- 状态栏提示当前正在编辑哪个 profile，以及点击会立即写入配置。

### 5. 启动行为

- Windows 产物必须使用 `-H=windowsgui` 编译，避免启动时出现黑色控制台窗口。
- 保留非 Windows 的控制台入口，方便兼容或调试。

### 6. 性能优化要求

- 后台只保留一个轻量 goroutine 做轮询。
- GUI 只保留一个 Win32 消息循环。
- 使用低优先级 + 后台模式 + EcoQoS。
- 不要为了 GUI 频繁扫描设备或重建复杂状态。
- 配置未变化时不重复应用硬件设置。
- GUI 改配置后通过轻量 wake channel 唤醒后台逻辑，而不是新开额外定时器。

## 视觉和体验要求

- 窗口不要太挤，适当加高一些。
- 默认字体不要用难看的系统 fallback。
- Windows 下优先使用：
  - `Microsoft YaHei UI`
  - 或 `Segoe UI`
- 托盘菜单中文化更友好：
  - `打开设置`
  - `退出`

## 迁移到别的项目时要告诉 Codex 的话

可以直接复制下面这段：

```text
请把这个 CLI 工具改成轻量级 Windows 托盘 GUI，要求：
1. 保留原有后台自动工作/自动切换逻辑。
2. Windows 启动时不要显示控制台窗口。
3. 用原生 Win32 或同等级轻量方案，不要上重型 GUI 框架。
4. 托盘左键显示/隐藏设置窗口，右键菜单至少有“打开设置”和“退出”。
5. GUI 里可以切换编辑 hit 和 miss/default 配置。
6. 用按钮或单选按钮图形化修改配置，点击后立刻写配置文件。
7. 配置文件仍然是真实来源，GUI 只是编辑器。
8. 要把后台核心逻辑和 GUI 外壳拆开。
9. 优先极低性能开销，避免重复轮询和重复下发设置。
10. 字体使用 Microsoft YaHei UI 或 Segoe UI。
```

## 本项目里已经落地的关键点

- Windows GUI 入口已经独立。
- 构建脚本已经改成 GUI 子系统。
- 托盘右键菜单包含退出。
- 设置窗口可以切换 `Hit` / `Miss(Default)`。
- 配置按钮点击后立即写文件。
- 后台自动切换逻辑仍然保留。

## 建议复用方式

- 把这份 note 直接复制到新项目。
- 让 Codex 先看这个 note，再读新项目现有配置结构和主循环。
- 迁移时优先复用“后台核心抽离 + 配置即写回 + 托盘外壳”这个模式。

